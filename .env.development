# 비워두면 Vite dev 서버의 proxy(localhost:3000 → localhost:8080)를 타고
# 같은 오리진으로 요청되어 CORS 가 발생하지 않음.
# 운영 빌드에서는 실 BE URL 로 채워야 함.
VITE_API_BASE_URL=
# 재고 화면에서 기본으로 조회할 창고 UUID (실 BE 모드에서 필요)
VITE_DEFAULT_WAREHOUSE_ID=
